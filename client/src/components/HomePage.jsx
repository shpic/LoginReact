import React from 'react';
import {Card,CardTitle} from 'material-ui/Card';

const HomePage = () => (
  <Card className="container">
    <CardTitle title="Login aplikacija" subtitle="Ovo je aplikacija za logovanje!"></CardTitle>
  </Card>
);

export default HomePage;
